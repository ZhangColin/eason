spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: true
        service-id: config
      profile: prod

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

logging:
  path: /var/log/eason/gateway/
  file: eason-gateway
