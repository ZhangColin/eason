spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: true
        service-id: config
      profile: staging

eureka:
  client:
    service-url:
      defaultZone: http://49.234.49.112:8761/eureka

logging:
  path: /var/log/eason/gateway/
  file: eason-gateway
