spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: staging

eureka:
  client:
    service-url:
      defaultZone: http://eason-eureka1:8761/eureka,http://eason-eureka2:8762/eureka

logging:
  path: /var/log/eason/goods/
  file: eason-goods
